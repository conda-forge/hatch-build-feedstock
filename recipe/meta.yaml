{% set name = "hatch-build" %}
{% set version = "0.3.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/hatch_build-{{ version }}.tar.gz
  sha256: 05742745fc2fd30284ae26402120385c5d4af65e252172dd9a9a5385051b33d7

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0
  entry_points:
    - hatch-build = hatch_build.cli:hatchling

requirements:
  host:
    - python {{ python_min }}
    - hatchling
    - pip
  run:
    - python >={{ python_min }}
    - hatchling >=1.14.1,<1.15

test:
  imports:
    - hatch_build.cli
  commands:
    - pip check
    - hatch-build --help
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/python-project-templates/hatch-build
  dev_url: https://github.com/python-project-templates/hatch-build
  summary: A minimal CLI wrapper around hatchling build
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - timkpaine
